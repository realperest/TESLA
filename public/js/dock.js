/**
 * Dock: önceki fareyle ikon büyütme / kaldırma efekti kaldırıldı.
 */
(function () {
  function init() {}
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
